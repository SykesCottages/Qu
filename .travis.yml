language: php

php:
  - '7.2'
  - '7.3'
  - nightly

services:
  - docker

before_install:
  - docker-compose up -d

install:
  - travis_retry composer install --no-interaction

script:
  - ./vendor/bin/grumphp run

cache:
  directories:
    - vendor
    - $HOME/.cache/composer